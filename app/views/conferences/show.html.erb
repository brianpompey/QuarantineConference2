<!DOCTYPE html>
<html>
<head>
<style>
ul {
  list-style-type: none;
  margin: 0;
  padding: 0;
  overflow: hidden;
  background-color: #333;
  position: -webkit-sticky; /* Safari */
  position: sticky;
  top: 0;
}

li {
  float: left;
}

li a {
  display: block;
  color: white;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
}

li a:hover {
  background-color: #111;
}
</style>
</head>
<body>
<% if current_user %>
    Logged in as <%= current_user.name %>
    <a href="/logout">LogOut</a>
<% else %>
<ul>
    <li><%= link_to "LogIn", login_path %></li>
    <li><%= link_to "SignUp", signup_path %></li>
</ul>
<% end %>

<h3>Specific information for the <%=@conference.name%> conference</h3>

    <dl>
        <dt><h2>Date</h2></dt>
        <dd><h4><%= @conference.date.strftime("%b %d") %></h4></dd>
        <dt><h2>Time</h2></dt>
        <dd><h4><%= @conference.time.strftime("%I:%M%p") %></h4></dd>
        <dt><h2>Minimum Donation</h2></dt>
        <dd><h4>$<%= @conference.min_donation %></h4></dd>
        <dt><h2>Keynote Speaker</h2></dt>
        <dd><h4><%= @conference.keynote %></h4></dd>
        <dt><h2>Category</h2></dt>
        <dd><h4><%= @conference.category %></h4></dd>
        <dt><h2>Workshops</h2></dt>
        <% @conference.workshops.each do |workshop| %>
            <dd><h4><%= workshop.name %> - <%= workshop.description %></h4></dd>
        <% end %>
    </dl>

    <% if current_user %>
        <button type="button" class="btn btn-default"><a href="/donations/new">Donation</a></button>
    <% else %>%
        <h3>SignUp/In to Register!</h3>
    <% end %>
    




</body>
</html>