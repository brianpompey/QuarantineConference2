<!DOCTYPE html>
<html>
<head>
<style>
</style>
</head>
<body>

<%= form_for model: Donation.new do |f| %>

    <div>
    <%= f.hidden_field :conference_id, :value => "#{@conference.id}" %>
    </div>

    <div>
    <%= f.hidden_field :user.id, :value => current_user.id %>
    </div>

    <div class="form-group">
    <%= f.label :amount, "Amount:" %>
    <%= f.number_field :amount, :value => "#{@conference.id}" %>
    </div>



    <div class="actions">
    <%= f.submit class:"btn btn-primary" %>
    </div>


<% end %>



</body>
</html>